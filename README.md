# LumaChain

LumaChain is a Web3/Blockchain project focused on the solar supply chain, built on the Stellar/Soroban network. 

## Deployed Contracts (Testnet)

The following smart contracts have been deployed to the Soroban Testnet:

*   **Solar Asset Contract**: `C... (Pending deployment due to local network timeout)`
*   **Registry Contract**: `C... (Pending deployment due to local network timeout)`
*   **Warranty Contract**: `C... (Pending deployment due to local network timeout)`
*   **Maintenance Log Contract**: `C... (Pending deployment due to local network timeout)`

*(Note: Actual contract IDs will be populated here once the network allows the deployment script to fetch the toolchains and deploy).*

## Components
- `/contracts`: Rust-based Soroban Smart Contracts
- `/sdk`: TypeScript SDK for interacting with the blockchain
- `/api`: Hono/Node REST API Gateway
